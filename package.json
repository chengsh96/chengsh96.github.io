{
  "name": "chengsh96-github-io",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website of Shihao Cheng — static GitHub Pages site.",
  "type": "module",
  "scripts": {
    "lint:html": "htmlhint \"index.html\" \"projects/**/*.html\" \"zh/**/*.html\"",
    "lint:js": "node scripts/check-js.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:nav": "node scripts/check-nav.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "check:i18n": "node scripts/check-i18n.mjs",
    "check:sitemap": "node scripts/gen-sitemap.mjs --check",
    "gen:sitemap": "node scripts/gen-sitemap.mjs",
    "test": "npm run lint:html && npm run lint:js && npm run check:links && npm run check:nav && npm run check:seo && npm run check:i18n && npm run check:sitemap"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4"
  }
}
